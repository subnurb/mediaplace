"""Infrastructure facades for platform integrations.

Re-exports from existing root-level service modules.
New code should import from here (e.g. ``from infrastructure.platforms import spotify``).
During incremental migration, the root-level modules remain canonical.
"""
